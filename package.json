{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "lint": "eslint ."
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.1.1"
  }
}
